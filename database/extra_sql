----USER TABLE PASSWORD Table-----
UPDATE ALL USER PASSWORD TO 1-8
1. UPDATE users SET users.password = '$2y$10$r59CptWXU.RaQvBiQv9/COzIZLJCFh3DLX7qnOGynJ7jZ/kQrawJq';

1. ALTER TABLE `wishers` CHANGE `guardian_name` `guardian_name` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL;
2. ALTER TABLE `wishers` CHANGE `guardian_phone` `guardian_phone` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL;
3. ALTER TABLE `wishers` CHANGE `relationship` `relationship` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL;

NEW Migrations
1. ALTER TABLE `users` ADD `guardian_name` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL AFTER `dob`;
2. ALTER TABLE `users` ADD `guardian_phone` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL AFTER `guardian_name`;
3. ALTER TABLE `users` ADD `relationship` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL AFTER `guardian_phone`;
4. ALTER TABLE `users` ADD `gender` ENUM('male','female','other') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'male' AFTER `relationship`;
5. ALTER TABLE `users` ADD `phone_verified_at` TIMESTAMP NULL DEFAULT NULL AFTER `email_verified_at`;

Set Admin role:
1. INSERT INTO role_user (id, role_id, user_id, created_at, updated_at) VALUES (NULL, '1', '1', NOW(), NOW()), (NULL, '2', '2', NULL, NULL)

fulfilment
1. ALTER TABLE `fulfillments` CHANGE `status` `status` ENUM('requested','accepted_by_wisher', 'accepted_by_donor','scheduled','in_progress','delivered','cancelled') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'requested';
2. ALTER TABLE `fulfillments` CHANGE `status` `status` ENUM('requested','accepted_by_wisher','accepted_by_donor','scheduled','in_progress','delivered', 'completed', 'cancelled') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'requested';

Users
1. ALTER TABLE `users` ADD `organization_id` BIGINT(20) UNSIGNED NULL DEFAULT NULL AFTER `address`;
2. ALTER TABLE `three_wish`.`users` ADD INDEX `users_organization_id_foreign` (`organization_id`);
3. ALTER TABLE `users` CHANGE `email` `email` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

Organizations
1. ALTER TABLE `organizations` ADD `user_id` BIGINT(20) UNSIGNED NULL DEFAULT NULL AFTER `description`;
2. ALTER TABLE `three_wish`.`organizations` ADD UNIQUE `organizations_user_id_foreign` (`user_id`);

wishes
1. ALTER TABLE `wishes` ADD `created_by` BIGINT(10) UNSIGNED NULL DEFAULT NULL AFTER `status`;

donations
1. ALTER TABLE `donations` ADD `created_by` BIGINT(10) UNSIGNED NULL DEFAULT NULL AFTER `status`;
